networks:
  expense: {}
volumes:
  mysql:
# docker run -d --name mysql --network expense -v mysql:/var/lib/mysql mysql:1.0.0
services:
  mysql:
    image: mysql:1.0.0
    networks:
      - expense
    volumes:
      - mysql:/var/lib/mysql